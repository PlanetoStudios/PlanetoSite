<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>PlanetoSite | Creator</title>
    <style>
        :root {
            --primary: #8a2be2;
            --secondary: #4b0082;
            --bg: #0b0b0d;
            --text: #ffffff;
            --accent: #00d4ff;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: var(--bg);
            color: var(--text);
            margin: 0;
            display: flex;
            flex-direction: column;
            align-items: center;
            min-height: 100vh;
        }

        header {
            width: 100%;
            padding: 40px 0;
            text-align: center;
            background: linear-gradient(180deg, var(--secondary) 0%, var(--bg) 100%);
        }

        /* Estilo da sua Foto de Perfil */
        .profile-pic {
            width: 150px;
            height: 150px;
            border-radius: 50%;
            border: 4px solid var(--accent);
            object-fit: cover;
            margin-bottom: 15px;
            box-shadow: 0 0 20px rgba(138, 43, 226, 0.5);
        }

        h1 { margin: 0; font-size: 2.5rem; letter-spacing: 2px; text-transform: uppercase; }
        .tagline { color: var(--accent); font-weight: bold; margin-top: 5px; }

        .container {
            max-width: 800px;
            width: 90%;
            padding: 20px;
            text-align: center;
        }

        .bio {
            font-size: 1.1rem;
            line-height: 1.6;
            margin: 25px 0;
            padding: 20px;
            border-left: 5px solid var(--primary);
            background: #16161a;
            border-radius: 0 15px 15px 0;
            text-align: left;
        }

        .ad-box {
            background: linear-gradient(45deg, #1e1e2e, #2a2a40);
            border: 2px solid var(--accent);
            border-radius: 20px;
            padding: 25px;
            margin: 30px 0;
        }

        .btn-planetoid {
            display: inline-block;
            background-color: var(--accent);
            color: #0b0b0d;
            padding: 12px 25px;
            border-radius: 30px;
            text-decoration: none;
            font-weight: bold;
            margin-top: 15px;
        }

        .contacts {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 15px;
            margin-top: 20px;
        }

        .contact-card {
            background: #1c1c21;
            padding: 15px;
            border-radius: 12px;
            border: 1px solid #333;
        }

        .contact-card strong { display: block; color: var(--accent); margin-bottom: 5px; }
        a { color: white; text-decoration: none; }

        footer { padding: 30px; font-size: 0.8rem; opacity: 0.5; }
    </style>
</head>
<body>

    <header>
        <img src="perfil.png" alt="Planeto" class="profile-pic">
        <h1>PLANETOSITE</h1>
        <p class="tagline">Animes • Debates • Jogos</p>
    </header>

    <div class="container">
        <section class="bio">
            <p>Sou um criador de conteúdo de animes, debates e jogos. Minha missão é explorar o universo geek e compartilhar as melhores experiências do mundo dos games.</p>
        </section>

        <section class="ad-box">
            <h2>PROJETO PLANETOID</h2>
            <p>Conheça o meu novo <strong>Game Hub</strong>! Confira o código no meu GitHub.</p>
            <a href="https://github.com/planetostudios/Planetoid" class="btn-planetoid" target="_blank">VER NO GITHUB</a>
        </section>

        <h3>CONTATOS</h3>
        <div class="contacts">
            <div class="contact-card">
                <strong>TikTok</strong>
                <a href="https://www.tiktok.com/@planetok11" target="_blank">@planetok11</a>
            </div>
            <div class="contact-card">
                <strong>Discord</strong>
                <span>@itzplaneto</span>
            </div>
            <div class="contact-card">
                <strong>E-mail</strong>
                <a href="mailto:yanguilhermecarvalhomaranduba@gmail.com">Enviar E-mail</a>
            </div>
        </div>
    </div>

    <footer>
        &copy; 2026 PlanetoSite • Criado por Planetostudios
    </footer>

</body>
</html>
